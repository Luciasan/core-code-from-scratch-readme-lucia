function NamedOne(first, last){
    this.firstName = first;
    this.lastName = last;
Object.defineProperty(this,"fullName",{
    return this.fullName + " " + this.lastName
}
this.fullName(name) {
const parent = name.strin
}
}
}
